{% extends '::base.html.twig' %}

{% block body -%}
    <h1>DireccionEmisor list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Calle</th>
                <th>Numeroext</th>
                <th>Numeroint</th>
                <th>Colonia</th>
                <th>Localidad</th>
                <th>Codigopostal</th>
                <th>Email</th>
                <th>Telefono</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('direccionemisor_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.calle }}</td>
                <td>{{ entity.numeroExt }}</td>
                <td>{{ entity.numeroInt }}</td>
                <td>{{ entity.colonia }}</td>
                <td>{{ entity.localidad }}</td>
                <td>{{ entity.codigoPostal }}</td>
                <td>{{ entity.email }}</td>
                <td>{{ entity.telefono }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('direccionemisor_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('direccionemisor_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('direccionemisor_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
