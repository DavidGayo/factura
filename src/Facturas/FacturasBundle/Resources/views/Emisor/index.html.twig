{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Emisor list</h1>

    
      <a id="new">nuevo</a>
      <div id="nuevo"> 
     
    </div>

    <table class="records_list">
        <thead>
            <tr>
                <th>Razonsocial</th>
                <th>Rfc</th>
                <th>Aciones</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('emisor_show', { 'id': entity.id }) }}">{{ entity.razonSocial }}</a></td>
                <td></td>
                <td>{{ entity.rfc }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('emisor_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('emisor_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('emisor_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}

    {% block js %}
    <script>

    $(document).ready(function(){

       $('#new').click(function(event){
            event.preventDefault();
            $('#nuevo').load("{{path ('emisor_new')}}");
        });
    });

    </script>

    {% endblock js%}
