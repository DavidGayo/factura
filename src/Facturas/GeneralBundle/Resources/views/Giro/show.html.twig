{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Giro</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Giro</th>
                <td>{{ entity.giro }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('giro') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('giro_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
